# ==========================================
# CONFIGURACAO OBRIGATORIA - ws7
# ==========================================
# 1. Copie este arquivo e renomeie para .env
# 2. Substitua a chave abaixo por uma chave segura (minimo 32 caracteres)
# 3. Para gerar uma chave automaticamente, execute no terminal:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# IMPORTANTE: NUNCA compartilhe este arquivo publicamente!
# ==========================================

FLASK_SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars
